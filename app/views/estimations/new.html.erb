<% javascript 'workadder' %> 
<% title "Create a new Request for Estimation for Task '#{@task.name}'" %>

<%= render :partial => 'form' %>

<p><%= link_to "Back to List", task_baselines_path(@task) %></p>
