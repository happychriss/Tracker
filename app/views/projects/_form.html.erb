<%= semantic_form_for @project do |f| %>
    <%= f.inputs do %>

        <%= f.input :name %>
        <%= f.input :description %>
        <%= f.input :organization if is_admin? %>
        
        <% if is_admin? then %>
		    <%= f.input :pm, :label=>'Projectmanager',  :include_blank => false ,:collection => Pm.find(:all, :order => 'username') %>
        <% end %>


    <% end %>
    <%= f.buttons %>

<% end %>


