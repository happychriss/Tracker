<% title "Edit Project" %>

<%= menu_bar do |mb|
        mb.menu_line "<<< Show Project Details", project_path(@project)
        mb.menu_line "All Projects", projects_path
end%>

<%= render :partial => 'form' %>

<%= link_to image_tag("/images/delete.png", :border =>0) + "Delete", @project, :confirm => 'Are you REALLY sure? The complete project will be deleted !!!!', :method => :delete if is_admin? %>

