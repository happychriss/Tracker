  <%= javascript_include_tag :defaults %>
<% title "Edit Project" %>

<%= menu_bar do |mb|
        mb.menu_line "<<< Show Project Details", project_path(@project)
        mb.menu_line "All Projects", projects_path
end%>

<%= render :partial => 'form' %>

<%= button_to "Delete", @project, :confirm => 'Are you REALLY sure? The complete project will be deleted !!!!', :method => :delete if is_admin? %>

